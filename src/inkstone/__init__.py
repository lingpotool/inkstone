"""inkstone —— 专业的跨平台 Python 原生 UI 系统。

设计目标：
    1. 纯 Python 编写用户代码，无构建步骤、无代码生成；
    2. 自绘渲染，界面外观 100% 由设计令牌驱动；
    3. Windows / macOS / Linux 三平台一等公民，任何平台不得长期降级支持；
    4. 中文一等公民：输入法、字体回退、标点禁则；
    5. 确定性：同样的输入永远得到同样的输出，因此可测试、可截图对比。

分层（上层依赖下层，禁止反向依赖）：

    L8  应用层        你的代码 / AI 生成的代码
    L7  组件库        60+ 生产级控件（由 primitives + 样式配方组合）
    L6.5 行为原语     headless：弹层 / 焦点 / 定位 / 解散
    L6  样式与主题    设计系统三层令牌 + 变体配方
    L5  组件树与状态  Widget / Element / RenderObject
    L4.5 动效引擎     缓动 / 弹簧 / 过渡 / 减少动效
    L4  布局引擎      约束向下、尺寸向上
    L3  文本与字体    整形、断行、回退、排版
    L2  渲染管线      显示列表（含阴影/渐变/图标）→ 可插拔光栅后端
    L1  输入与无障碍  指针/键盘/IME/焦点/语义树
    L0  平台抽象层    Windows / macOS / Linux 差异在这里被吃掉

当前状态：架构设计阶段，公开 API 尚未稳定。详见 ROADMAP.md。
"""

from ._version import __version__

__all__ = ["__version__"]
